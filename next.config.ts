// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  // Optional: Use this if you deploy under a sub-path
  // basePath: '/your-base-path',
  reactStrictMode: true,
}

module.exports = nextConfig
